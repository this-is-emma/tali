extends layout

block content
  .row
    .col-sm-4
      if item.picUrlSq
        img(src=item.picUrlSq).img-fluid
    .col-sm-8
      h1=item.name
      p=item.type
      p=item.price
      p=item.description
      .text-right
        a(href="/items/" + item.id + "/edit").btn-link Edit
      
